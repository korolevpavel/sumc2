импорт Закупки


метод ВсеСлужбыДоставкиGETОбработкаЗапроса(Запрос: HttpСервисЗапрос)
    знч СлужбаДоставки = СлужбыДоставки.ПолучитьВсеСлужбыДоставки()
    Запрос.Ответ.Заголовки.Установить("Content-type", "application/json")
    Запрос.Ответ.УстановитьКодСтатуса(200)
    Запрос.Ответ.УстановитьТело(СериализацияJson.ЗаписатьОбъект(СлужбаДоставки))
;

метод СлужбыДоставкиПоСсылкеGETОбработкаЗапроса(Запрос: HttpСервисЗапрос)
    пер СлужбаДоставкиСсылка = Запрос.Параметры.ПолучитьПервый("Link")
    знч СлужбаДоставки = СлужбыДоставки.ПолучитьСлужбуДоставкиПоСсылке(СлужбаДоставкиСсылка)
    Запрос.Ответ.Заголовки.Установить("Content-type", "application/json")
    Запрос.Ответ.УстановитьКодСтатуса(200)
    Запрос.Ответ.УстановитьТело(СериализацияJson.ЗаписатьОбъект(СлужбаДоставки))
;