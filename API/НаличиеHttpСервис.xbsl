импорт Закупки


метод ПолучитьНаличиеGETОбработкаЗапроса(Запрос: HttpСервисЗапрос)
    знч ОтчетНаличия = Наличие.ПолучитьАктуальноеНаличие()
    Запрос.Ответ.Заголовки.Установить("Content-type", "application/json")
    Запрос.Ответ.УстановитьКодСтатуса(200)
    Запрос.Ответ.УстановитьТело(СериализацияJson.ЗаписатьОбъект(ОтчетНаличия))
;

метод ТоварПоСсылкеGETОбработкаЗапроса(Запрос: HttpСервисЗапрос)
    пер ТоварСсылка = Запрос.Параметры.ПолучитьПервый("Link")
    знч ОтчетНаличия = Наличие.ПолучитьАктуальноеНаличиеПоСсылкеТовара(ТоварСсылка)
    Запрос.Ответ.Заголовки.Установить("Content-type", "application/json")
    Запрос.Ответ.УстановитьКодСтатуса(200)
    Запрос.Ответ.УстановитьТело(СериализацияJson.ЗаписатьОбъект(ОтчетНаличия))
;